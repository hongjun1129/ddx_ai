{
  "name": "chest-pain-workflow-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build:data": "node scripts/build-clinical-data.mjs",
    "build:public": "node scripts/build-public.mjs",
    "build": "npm run build:data && npm run build:public",
    "start": "node server.js",
    "dev": "node server.js"
  }
}
